# strix shell-access policy (R4d/R5): key-only on every path, root never.
# Fedora's stock sshd_config permits password auth — these overrides are
# load-bearing, not decorative. mosh authenticates through this same sshd.
PermitRootLogin no
PasswordAuthentication no
KbdInteractiveAuthentication no
