# libvirt state and config both live on the data drive (R7): wait for the
# strix-vm mount AND the /etc/libvirt bind before the daemon reads either.
[Unit]
RequiresMountsFor=/var/lib/libvirt /etc/libvirt
